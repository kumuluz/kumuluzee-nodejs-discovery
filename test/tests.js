const expect = require("chai").expect;

const ConfigurationUtil = require("kumuluzee-config").ConfigurationUtil;

const KumuluzeeDiscovery = require("../dist/index").default;

describe("get()", () => {
    it("should pass tests, because there are none", () => {
        expect(1).to.be.equal(1);
    });
});
